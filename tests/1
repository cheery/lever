ffi = import("ffi")

data = ffi.automem(ffi.float, 6)
i = 0
for x in [0.0, 0.0, 1.0, 0.0, 1.0, 1.0]
    data[i] = x
    i = i + 1

print(data)

# There was a bug in chaining rules
# but the language has no chaining rules yet.
# reset_round = true
# x = 5
# (if reset_round and 0 < x < 10)
#     (print "a")
# (else)
#     (if reset_round)
#         (print "b")
