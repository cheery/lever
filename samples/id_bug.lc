# This is probably not just a bug, but a feature not implemented.
# I thought this was already implemented though.
# .. Indeed, a method was missing that was necessary for this to work.

a = object()

blok = {}

blok[Id(a)] = 10

print(blok[Id(a)])
