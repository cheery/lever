(= dlopen ffi.dlopen)
(= cdef   ffi.cdef)
(= long   ffi.long)

(= lib (dlopen "libSDL.so"))

(cdef lib.SDL_Init long  long)
(cdef lib.SDL_SetVideoMode long  long long long long)

(print "trying out")
(print "SDL_Init ->" (lib.SDL_Init 65535))
(= screen (lib.SDL_SetVideoMode 640 480 32 0))
