main = ():
    sg = generator
        for x in range(10)
        for y in range(20)
        repeat
            yield x * y

    s = 0
    for x in sg repeat
        s = s + x
    print('simple_generator output sum:', s)
