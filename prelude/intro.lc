test = ():
    x = 0
    while x < 15 repeat
        print(x)
        x += 1

    x = 0
    while x < 15 repeat
        print(x)
        x (jittery_op)= 1

jittery_op = (a, b):
    if a & 1 == 1 then
        return a + b * -1
    else
        return a + b * +3

test()
