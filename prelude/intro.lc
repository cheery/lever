import graph_utils

f = face(graph_utils.greedy_biclique_decomposition)
print('faa', f)
for x in f.params() repeat
    print('bar', x)

#datatype State(a) = state(
#    Integer, Set(a), Set(State(a)), Dict(Parameter, State(a)))
